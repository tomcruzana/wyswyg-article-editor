------- CREATE A COURSE
		CourseBuilder cb = new CourseBuilder();
		Course sbCourse = cb.prepareCourse("Spring Boot");
		Chapter sbChapter1 = cb.prepareChapter(sbCourse, "Chapter 1", 1);
		cb.addChapterToCourse(sbCourse, sbChapter1);
		Page sbPage1 = cb.preparePage(sbChapter1, "Section 1", 1);
		cb.addPagetoChapter(sbChapter1, sbPage1);
		System.out.println("LOG: " + sbCourse);
		
------- PERSIST A COURSE
		CourseDaoImpl cDao = new CourseDaoImpl();
		cDao.add(sbCourse);
		
		ChapterDaoImpl chDao = new ChapterDaoImpl();
		chDao.add(sbChapter1);
		
		PageDaoImpl pDao = new PageDaoImpl();
		pDao.add(sbPage1);
		
		cDao.addChapterToCourse(sbChapter1, sbCourse);
		chDao.addPagetoChapter(sbPage1, sbChapter1);